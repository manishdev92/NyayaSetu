[pytest]
# NS-S1-05: marker for a subset of tests (opt-in; most tests are unmarked)
markers =
  routing: routing, authority, hybrid, priority, emergency (safety slice)
  rag: RAG, Pinecone adapter, retrieval pipeline, statute ingest (vector-store slice)

testpaths = tests
